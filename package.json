{
  "name": "motion-converter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Convert motion files (MOV, MP4, WebM, GIF, Lottie JSON) between formats, entirely in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "fflate": "^0.8.2",
    "lottie-web": "^5.12.2"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
